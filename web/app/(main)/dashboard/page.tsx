'use client';

import styles from "./dashboard.module.scss";

export default function DashboardPage() {
  return (
    <div className={styles.page}>
      <div className={styles.content}>
        {/* phần content dashboard sẽ làm tiếp sau */}
      </div>
    </div>
  );
}
